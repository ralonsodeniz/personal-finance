{
  "name": "personal-finance",
  "version": "0.0.0",
  "private": true,
  "packageManager": "pnpm@11.21.0",
  "engines": {
    "node": ">=24.18.0 <25"
  },
  "scripts": {
    "workspaces": "pnpm list --recursive --depth -1",
    "task": "turbo run placeholder --cache-dir .turbo/cache"
  },
  "devDependencies": {
    "turbo": "2.10.9"
  }
}
